import React from 'react';
import MDXComponents from '@theme-original/MDXComponents';
// ChapterActions disabled - using ChapterPersonalizeWrapper instead to avoid duplicate buttons
// import ChapterActions from '@site/src/components/ChapterActions';

export default {
  ...MDXComponents,
  // ChapterActions, // Disabled to prevent duplicate personalize buttons
};

